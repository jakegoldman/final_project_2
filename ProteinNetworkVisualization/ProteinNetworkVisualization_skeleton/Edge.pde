class Edge {
  Node first, second; //nodes that the edge is connecting
  PVector sForce; //spring force
  int springL; //rest length
  
  Edge(Node a, Node b) {
    first = a;
    second = b;
  }
  
  
  
  
  
}